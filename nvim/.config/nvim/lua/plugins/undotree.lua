return {
  {
    "mbbill/undotree",
    config = function()
      -- You can add any specific configuration here if needed.
      -- By default, pressing `<leader>u` will open the undo tree.
    end,
  },
}
